Download the Zip file
create database called demodb
and create the tables whatever it included
install the packages from the terminal
npm i pg express body-parser
run the index.js on the terminal
got to the browser and search https://localhost:3000
the output will be visible on the screen
